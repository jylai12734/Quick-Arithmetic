#ifndef FLUENCY
#define FLUENCY

#include "arithmetic.h"
#include "utils.h"

void show(vector<vs>& problems);

void fluencydrill(int& end, int& correct);

void fluency(map<string, int>& fData, pair<string, int>& fTop);

#endif // FLUENCY